# Callisto v0.9 M3 Docs, Samples + Editor Drift Checklist

## Docs

- [x] README planning links include v0.9 and v1.0 readiness docs.
- [x] Cheat sheet describes the frozen prelude, list indexing, and helper forms.
- [x] VS Code extension README describes the v0.9 compatibility-frozen surface.
- [x] Zed extension README describes the v0.9 compatibility-frozen surface.
- [x] Existing roadmap continues to describe v0.9 as compatibility and stabilization.

## Samples And Templates

- [x] Maintained samples use brace-delimited syntax.
- [x] Playdate template source uses brace-delimited syntax.
- [x] Docs avoid recommending project-local `Option` definitions.

## Editor Fixtures

- [x] VS Code highlighting fixture covers `List[T]`, `Option[T]`, list indexing, `map`, `append`, `filter`, and `fold`.
- [x] Zed highlighting fixture covers `List[T]`, `Option[T]`, list indexing, `map`, `append`, `filter`, and `fold`.
- [x] Editor regression commands are part of the release gate.
