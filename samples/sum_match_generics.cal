module sum_match_generics

fn id[T](x: T) -> T {
  x
}

pub fn choose(flag: Bool, a: Int, b: Int) -> Int {
  var picked: Option[Int] = Some(id(a))
  if flag {
    picked = Some(id(a))
  } else {
    picked = Some(id(b))
  }

  match picked {
    case Some(v) => v
    case None    => 0
  }
}
